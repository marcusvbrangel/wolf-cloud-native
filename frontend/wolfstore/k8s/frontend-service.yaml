apiVersion: v1
kind: Service
metadata:
  name: wolfstore-frontend-service
  namespace: wolfns
spec:
  selector:
    app: wolfstore-frontend
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 80
  type: LoadBalancer
